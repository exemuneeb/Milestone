{
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "pwd && ls && pnpm --filter @workspace/benchboard run build && echo BUILD_DONE && ls artifacts/benchboard/dist && rm -rf public && cp -r artifacts/benchboard/dist/public public",
  "outputDirectory": "public",
  "functions": { "api/index.js": { "maxDuration": 30 } },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
